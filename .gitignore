node_modules/
dist/
output/

.env
.env.local

# Personal data. data/profile.example.yaml is the committed template.
data/profile.yaml
curriculos/
jobs/
memory/
# Reference CV holding real profile data; templates/ is the committed source of truth.
cv.html
# Personal ops cheat-sheet for real applications.
COMANDOS.md
# Local search state: fetched postings and the last search's ordering.
data/postings.cache.json
# Board tokens probed by `discover`; a dead slug is remembered for 30 days.
data/discovery.cache.json
# The tracker database: your real applications and their outcomes.
data/tracker.db
data/tracker.db-shm
data/tracker.db-wal

# Gmail OAuth: your desktop credentials and the cached read-only token.
# Both are secrets and neither belongs in git.
data/gmail-credentials.json
data/gmail-token.json

*.log
npm-debug.log*
.DS_Store
.vitest/
coverage/
